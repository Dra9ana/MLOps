# python_project

Minimal Python project skeleton.

How to run the small smoke test (PowerShell):

```powershell
cd C:\Users\Dragana\Desktop\MLOps\python_project
python tests\test_basic.py
```

Or, if you prefer pytest (install with `pip install pytest`):

```powershell
python -m pytest -q
```
